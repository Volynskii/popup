import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import App from './App';

const Appe = (
        <App/>
);
ReactDOM.render(
    Appe,
    document.getElementById('root')
);
